[build]
  publish = "."
  functions = "netlify/functions"

[dev]
  port = 8888
  publish = "."

# Redirect /api/* to serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
